# Xenon-scanner
This is xploit tools like hydra, but Simpler

and to use it use this command to install it

# pkg install wget -y

# wget https://github.com/void-scripter/Xenon-scanner/raw/main/xenon_1.0_all.deb && sudo apt install ./xenon_1.0_all.deb